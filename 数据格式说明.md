# Kafka 数据格式说明

本项目支持两种 Kafka 数据格式：

## 格式1：单个车辆点格式（新格式）

这是当前主要使用的格式，直接是一个车辆点对象。

### 必需字段
- `latitude` (Double): 纬度
- `longitude` (Double): 经度

### 可选字段
- `id` (Long/Integer/String): 车辆ID
  - 如果是数字类型，直接使用
  - 如果是字符串类型，会使用当前时间戳作为ID
  - 如果不存在，会自动生成时间戳ID
- `plateNo` (String): 车牌号
  - 如果不存在，会生成 "未知" + 随机数字
- `direction` (Integer): 方向
  - 如果不存在，默认为 1
- `stakeId` (String): 桩号
  - 如果不存在，会尝试使用 `rampStake` 字段
  - 如果都不存在，默认为空字符串
- `rampStake` (String): 匝道桩号（如果 `stakeId` 不存在时使用）
- `source` (Integer): 数据来源
  - `99`: 预测点（前端显示为绿色）
  - 其他值: 普通点（前端显示为橙色/红色/蓝色）
- `ramp` (String): 匝道标识（如 "A", "B", "C"）
- `mileage` (Double): 里程（米）
- `speed` (Double): 速度（米/秒）
- `heading` (Double): 航向角
- `deviceId` (Integer): 设备ID
- `laneNo` (Integer): 车道号
- `color` (Integer): 颜色标识
- `altitude` (Double): 海拔高度
- `cameraId` (Integer): 摄像头ID
- `type` (Integer): 类型

### 示例1：完整格式
```json
{
  "color": 0,
  "deviceId": 4,
  "heading": 262.0,
  "id": 26405,
  "laneNo": 1,
  "latitude": 30.919164657592773,
  "longitude": 114.041015625,
  "mileage": 984.0,
  "plateNo": "默A00000",
  "ramp": "A",
  "rampStake": "K1122+200-AK0+984",
  "source": 99,
  "speed": 2025.0
}
```

### 示例2：字符串ID格式
```json
{
  "altitude": 0.0,
  "cameraId": 0,
  "color": 0,
  "deviceId": 4,
  "heading": 245.0,
  "id": "26408",
  "laneNo": 1,
  "latitude": 30.9194935,
  "longitude": 114.0430114,
  "mileage": 395.5,
  "plateNo": "默A00000",
  "ramp": "C",
  "rampStake": "K1122+200-CK0+395.5",
  "source": 2,
  "speed": 1739.0,
  "type": 1
}
```

### 示例3：最小格式（仅必需字段）
```json
{
  "latitude": 30.919164657592773,
  "longitude": 114.041015625
}
```

---

## 格式2：pathList 格式（旧格式）

这是一个包装格式，包含一个 `pathList` 数组，数组中包含多个车辆点对象。

### 结构
```json
{
  "timeStamp": "2025-12-26T10:11:37.009",
  "pathList": [
    {
      "id": 17962,
      "plateNo": "测A17962",
      "longitude": 114.04424285888672,
      "latitude": 30.919843673706055,
      "direction": 1,
      "stakeId": "K1122+200-AK0+285",
      "source": 999
    },
    {
      "id": 20001,
      "plateNo": "测C20001",
      "longitude": 114.04366302490234,
      "latitude": 30.919771194458008,
      "direction": 1,
      "stakeId": "K1122+200-CK0+325.5",
      "source": 999
    }
  ]
}
```

### 字段说明
- `timeStamp` (String): 时间戳字符串，会应用到 `pathList` 中的所有车辆点
- `pathList` (Array): 车辆点数组
  - 数组中的每个对象包含与"单个车辆点格式"相同的字段
  - 每个点的 `timeStampStr` 会被设置为外层的 `timeStamp` 值

---

## 字段映射规则

### ID 字段处理
- 如果 `id` 是数字类型（Long/Integer），直接使用
- 如果 `id` 是字符串类型，会使用当前时间戳（`System.currentTimeMillis()`）作为ID
- 如果 `id` 不存在，会自动生成时间戳ID

### 桩号字段处理
- 优先使用 `stakeId` 字段
- 如果 `stakeId` 不存在，使用 `rampStake` 字段
- 如果都不存在，默认为空字符串

### 方向字段处理
- 如果 `direction` 不存在，默认为 `1`

### 车牌号字段处理
- 如果 `plateNo` 不存在，会生成 "未知" + 随机数字（时间戳取模10000）

---

## 数据来源标识（source 字段）

- `source = 99`: **预测点**
  - 前端显示：绿色标签和绿色位置点
  
- `source = 其他值`（如 2, 999 等）: **普通点**
  - 前端显示：
    - 标签：橙色
    - 位置点：根据 `direction` 字段
      - `direction = 1`: 红色
      - `direction ≠ 1`: 蓝色

- `source = null`: **未知来源**
  - 前端显示：按普通点处理

---

## 识别逻辑

代码按以下顺序识别数据格式：

1. **首先检查是否有 `pathList` 字段**
   - 如果有且不为空 → 按 **格式2（pathList格式）** 处理
   
2. **然后检查是否有 `latitude` 和 `longitude` 字段**
   - 如果有 → 按 **格式1（单个车辆点格式）** 处理
   
3. **如果都不满足**
   - 输出警告：无法识别的JSON格式

---

## 注意事项

1. **必需字段**：`latitude` 和 `longitude` 是处理单个车辆点格式的必需字段
2. **ID 处理**：字符串类型的 `id` 会被转换为时间戳，可能导致同一辆车被识别为不同车辆
3. **时间戳**：单个车辆点格式如果没有外层 `timeStamp`，会使用当前时间
4. **字段兼容性**：代码会自动处理字段名的差异（如 `stakeId` vs `rampStake`）

